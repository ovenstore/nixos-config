{
  environment.sessionVariables = {
    # Set the default text editor
    EDITOR = "hx";

    # Set cursor theme (Niri handles its own cursor)
    XCURSOR_THEME = "Bibata-Original-Classic";
    XCURSOR_SIZE = "20";
  };
}
